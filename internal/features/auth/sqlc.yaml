version: "2"
sql:
  - schema: "migrations/"
    queries: "."
    engine: "sqlite"
    gen:
      go:
        package: "auth"
        out: "."
        emit_json_tags: true
        emit_interface: true

# ===========================================
# GOTH Stack - Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env e preencha os valores

# -------------------------------------------
# Servidor
# -------------------------------------------

# Porta do servidor HTTP (padrão: 8080)
PORT=8080

# URL base da aplicação (usada em emails)
# Em desenvolvimento: http://localhost:8080
# Em produção: https://seudominio.com
BASE_URL=http://localhost:8080

# Ambiente da aplicação: "dev" ou "prod"
# Em produção, algumas validações stricter são aplicadas
APP_ENV=dev

# Versão da aplicação (usado nos logs)
VERSION=dev

# -------------------------------------------
# Logging e Observabilidade
# -------------------------------------------

# Commit hash do Git (definido automaticamente em CI/CD)
# Para desenvolvimento local: export COMMIT_HASH=$(git rev-parse HEAD)
COMMIT_HASH=unknown

# Região do datacenter (us-east-1, sa-east-1, etc.)
REGION=local

# ID da instância (hostname em produção)
INSTANCE_ID=unknown

# -------------------------------------------
# Banco de Dados
# -------------------------------------------

# URL do banco de dados SQLite
# Use :memory: para testes em memória
# Use ./goth.db para arquivo local
DATABASE_URL=./goth.db

# -------------------------------------------
# Sessão
# ------------------------------------------

# Secret para encriptar sessões
# IMPORTANTE: Em produção, use uma string aleatória segura
# Gerar: openssl rand -base64 32
SESSION_SECRET=

# -------------------------------------------
# SMTP (E-mail)
# ------------------------------------------

# Host do servidor SMTP (padrão: localhost)
SMTP_HOST=localhost

# Porta do servidor SMTP (padrão: 1025 para desenvolvimento)
SMTP_PORT=1025

# Usuário SMTP (obrigatório em produção)
SMTP_USER=

# Senha SMTP (obrigatório em produção)
SMTP_PASS=

# Endereço de e-mail remetente (padrão: noreply@goth.com)
SMTP_FROM=noreply@goth.com

# -------------------------------------------
# Observabilidade (OpenTelemetry + Grafana Tempo)
# -------------------------------------------

# Endpoint do collector OTLP (ex: tempo:4317)
# Deixe vazio para desabilitar tracing
OTEL_EXPORTER_OTLP_ENDPOINT=

# -------------------------------------------
# Exemplo de configuração para produção
# ------------------------------------------
# APP_ENV=prod
# DATABASE_URL=./goth.db
# BASE_URL=https://seudominio.com
# SESSION_SECRET=sua-chave-secreta-aqui
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu-email@gmail.com
# SMTP_PASS=sua-senha-de-app
# SMTP_FROM=noreply@seudominio.com
#
# # Logging (definidos automaticamente em CI/CD)
# VERSION=1.0.0
# COMMIT_HASH=abc123def456
# REGION=sa-east-1
# INSTANCE_ID=i-0123456789abcdef0

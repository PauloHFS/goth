package components

// LucideIcon creates a Lucide icon element (requires Lucide CDN script)
// Usage: @components.LucideIcon("mail", "h-5 w-5 text-gray-400")
templ LucideIcon(name string, class string) {
	<i data-lucide={ name } class={ class }></i>
}
